@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  color: #111827;
  background-color: #f4efe7;
  --fw-logo-green: #41a65b;
  --fw-logo-green-light: #6ecf87;
  --fw-logo-green-dark: #2f7c44;
  --fw-forest: #1f4d36;
  --fw-leaf: #3c7a4a;
  --fw-sand: #f4efe7;
}

html {
  scroll-behavior: smooth;
}

/* Screen reader only utility */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}

.sr-only:focus,
.sr-only:active {
  position: static;
  width: auto;
  height: auto;
  overflow: visible;
  clip: auto;
  white-space: normal;
}

/* Respect reduced motion preference */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
  
  .fade-in-up,
  .fade-in-up-delay-1,
  .fade-in-up-delay-2,
  .fade-in-up-delay-3,
  .fade-in-up-delay-4,
  .fade-in-hidden {
    animation: none !important;
    opacity: 1 !important;
    transform: none !important;
    filter: none !important;
  }
}

body {
  min-height: 100vh;
  background-color: #f4efe7;
}

/* Allow selection and interaction in interactive elements */
button,
a,
input,
textarea,
select,
[contenteditable="true"],
[role="button"],
[role="link"],
summary {
  -webkit-user-select: text;
  -moz-user-select: text;
  -ms-user-select: text;
  user-select: text;
  cursor: pointer;
  pointer-events: auto !important;
  -webkit-tap-highlight-color: rgba(0, 0, 0, 0.1);
}

/* Ensure buttons and links are always clickable */
button:not(:disabled),
a[href] {
  pointer-events: auto !important;
  cursor: pointer !important;
}

::selection {
  background-color: #3c6e47;
  color: #f4efe7;
}

/* Premium fade-in animation utilities */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
    filter: blur(4px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
    filter: blur(0);
  }
}

/* Premium fade-in - safe initial state that doesn't break layout */
.fade-in-up {
  will-change: opacity, transform, filter;
  animation: fadeInUp 0.9s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

.fade-in-up-delay-1 {
  will-change: opacity, transform, filter;
  animation: fadeInUp 0.9s cubic-bezier(0.16, 1, 0.3, 1) 0.15s forwards;
}

.fade-in-up-delay-2 {
  will-change: opacity, transform, filter;
  animation: fadeInUp 0.9s cubic-bezier(0.16, 1, 0.3, 1) 0.3s forwards;
}

.fade-in-up-delay-3 {
  will-change: opacity, transform, filter;
  animation: fadeInUp 0.9s cubic-bezier(0.16, 1, 0.3, 1) 0.45s forwards;
}

.fade-in-up-delay-4 {
  will-change: opacity, transform, filter;
  animation: fadeInUp 0.9s cubic-bezier(0.16, 1, 0.3, 1) 0.6s forwards;
}

/* Initial hidden state - only apply when animation hasn't started */
.fade-in-hidden {
  opacity: 0;
  transform: translateY(20px);
  filter: blur(4px);
}

/* sdg layout refine styles */
.sdg-glass {
  background: rgba(255, 255, 255, 0.6);
  border: 1px solid rgba(255, 255, 255, 0.5);
  -webkit-backdrop-filter: blur(10px);
  backdrop-filter: blur(10px);
  border-radius: 14px;
  box-shadow: 0 8px 22px rgba(17, 24, 39, 0.08);
  transition: transform 240ms cubic-bezier(0.2, 0.8, 0.2, 1), box-shadow 240ms ease, background 240ms ease;
  will-change: transform;
}
.sdg-glass:hover {
  transform: translateY(-8px) scale(1.025);
  box-shadow: 0 20px 40px rgba(17, 24, 39, 0.14);
  background: rgba(255, 255, 255, 0.78);
}
.sdg-badge-img {
  width: 72px;
  height: 72px;
  object-fit: contain;
  display: inline-block;
}
.sdg-badge-img-sm {
  width: 56px;
  height: 56px;
}
.sdg-card-title {
  font-weight: 600;
  font-size: 0.95rem;
}
.sdg-card-desc {
  font-size: 0.85rem;
  color: #4b5563;
}
.sdg-primary-card {
  padding: 28px;
  min-height: 120px;
  display: flex;
  align-items: center;
  gap: 20px;
}
.sdg-support-row {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 18px;
  align-items: start;
}
.sdg-support-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px;
  border-radius: 10px;
}
@media (max-width: 768px) {
  .sdg-support-row { grid-template-columns: 1fr; }
  .sdg-badge-img { width: 56px; height: 56px; }
  .sdg-primary-card { padding: 18px; }
}

/* sdg-alt tweaks */
.sdg-glass .w-20 { width: 80px; height: 80px; }
.sdg-glass p { margin: 0; }

/* sdg-alt-tweak styles */
.sdg-logo-wrap {
  display: inline-block;
  border-radius: 12px;
  padding: 6px;
  transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1), box-shadow 220ms ease;
}
.sdg-grid-logo {
  width: 112px;
  height: 112px;
  object-fit: contain;
  display: block;
  transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1), filter 220ms ease;
  will-change: transform;
  border-radius: 6px;
}
.sdg-logo-wrap:hover .sdg-grid-logo {
  transform: translateY(-6px) scale(1.08);
  filter: drop-shadow(0 18px 30px rgba(17, 24, 39, 0.15));
}
@media (max-width: 900px) {
  .sdg-grid-logo { width: 96px; height: 96px; }
}
@media (max-width: 640px) {
  .sdg-grid-logo { width: 72px; height: 72px; }
  .sdg-logo-wrap { padding: 4px; }
  .sdg-alt-tile.sdg-glass { padding: 12px; }
}

/* sdg-grouped styles */
.sdg-grouped-logo {
  width: 96px;
  height: 96px;
  object-fit: contain;
  display: block;
  margin: 0 auto;
  transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1), filter 220ms ease;
}
.sdg-glass .sdg-logo-wrap { display: inline-block; border-radius: 8px; padding: 6px; }
.sdg-glass .sdg-logo-wrap:hover .sdg-grouped-logo {
  transform: translateY(-6px) scale(1.06);
  filter: drop-shadow(0 18px 30px rgba(17, 24, 39, 0.12));
}
@media (max-width: 900px) {
  .sdg-grouped-logo { width: 84px; height: 84px; }
}
@media (max-width: 640px) {
  .sdg-grouped-logo { width: 64px; height: 64px; }
}

/* sdg-logos-only styles */
.sdg-logo-only-img {
  width: 112px;
  height: 112px;
  object-fit: contain;
  display: block;
  transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1), filter 220ms ease;
  will-change: transform;
  margin: 0 auto;
}
.sdg-logo-btn { background: none; border: none; outline: none; }
.sdg-logo-btn:focus {
  outline: 2px solid rgba(0, 0, 0, 0.08);
  outline-offset: 4px;
  border-radius: 8px;
}
.sdg-logo-btn:hover .sdg-logo-only-img,
.sdg-logo-btn:focus .sdg-logo-only-img {
  transform: translateY(-6px) scale(1.08);
  filter: drop-shadow(0 18px 30px rgba(17, 24, 39, 0.12));
}
@media (max-width: 900px) {
  .sdg-logo-only-img { width: 96px; height: 96px; }
}
@media (max-width: 640px) {
  .sdg-logo-only-img { width: 72px; height: 72px; }
}

/* sdg-distinguish styles */
.sdg-logo-primary-img {
  width: 128px;
  height: 128px;
  object-fit: contain;
  display: block;
  transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1), filter 220ms ease;
  will-change: transform;
  border-radius: 8px;
}
.sdg-logo-btn-primary {
  padding: 8px;
  border-radius: 18px;
  box-shadow: 0 8px 18px rgba(17, 24, 39, 0.06);
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.65), rgba(255, 255, 255, 0.55));
  border: none;
  outline: none;
  cursor: pointer;
}
.sdg-logo-btn-primary:hover .sdg-logo-primary-img,
.sdg-logo-btn-primary:focus .sdg-logo-primary-img {
  transform: translateY(-8px) scale(1.1);
  filter: drop-shadow(0 22px 36px rgba(17, 24, 39, 0.16));
}

.sdg-logo-support-img {
  width: 88px;
  height: 88px;
  object-fit: contain;
  display: block;
  transition: transform 200ms cubic-bezier(0.2, 0.8, 0.2, 1), filter 200ms ease;
  will-change: transform;
}
.sdg-logo-btn-support {
  background: none;
  border: none;
  outline: none;
  cursor: pointer;
}
.sdg-logo-btn-support:hover .sdg-logo-support-img,
.sdg-logo-btn-support:focus .sdg-logo-support-img {
  transform: translateY(-4px) scale(1.06);
  filter: drop-shadow(0 12px 22px rgba(17, 24, 39, 0.1));
}

@media (max-width: 1100px) {
  .sdg-logo-primary-img { width: 112px; height: 112px; }
  .sdg-logo-support-img { width: 72px; height: 72px; }
}
@media (max-width: 640px) {
  .sdg-logo-primary-img { width: 88px; height: 88px; }
  .sdg-logo-support-img { width: 64px; height: 64px; }
}

